	<% if !@source %>
		<div class="container">
		<div class="row">
			<div class="col-md-2 col-xs-0"></div>
			<div class="col-xs-6 col-md-3">
				<%= link_to("New Game", new_game_path) if current_user %>
			</div>
			<div class="col-xs-6 col-md-3">
			<% if current_user %>
				<%= link_to "log out", destroy_user_session_path, method: :delete %>
			<% else %>
				<%= link_to "log in", new_user_session_path %>
			<% end %>
			</div>
		</div>
		</div>
	<% end %>